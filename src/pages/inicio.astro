---
import { getImage } from "astro:assets";
import ElegantLayout from "@layouts/ElegantLayout.astro";
import myBackground from "@assets/chica1.png";

const optimizedBackground = await getImage({
	src: myBackground,
	format: "webp",
});
---

<ElegantLayout
  title="Inicio - Cosmopolitican"
  background={`url(${optimizedBackground.src})`}
>
  <div class="hero-section">
    <span class="block-header">Nueva era</span>

    <div class="massive-title">
      <div class="ornament-bg">C</div>
      <span class="line line-1">Tu revista</span>
      <span class="line line-2">CON CLASE</span>
      <span class="line line-3">Cosmopolitican</span>
    </div>

    <div class="divider"></div>

    <p class="hero-text">
      Lo que no puede faltar en la cartera de <em>la dama</em>
      ni el bolsillo del <em>varón</em>. Tu revista con clase.
    </p>

    <a href="/revista" class="cta-button">
      Leer el número actual
      <svg
        width="16"
        height="16"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
      >
        <path d="M5 12h14M12 5l7 7-7 7"></path>
      </svg>
    </a>
  </div>
</ElegantLayout>

<style>
  @import url("https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;0,900;1,400;1,700&family=EB+Garamond:ital,wght@0,400;0,500;1,400;1,500&display=swap");

  .hero-section {
    font-family: "EB Garamond", Georgia, serif;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    max-width: 900px;
    margin: 0 auto;
    padding: 3rem 2.5rem 3.5rem;
    position: relative;
    background-size: cover;
  }

  .block-header {
    font-size: 0.78rem;
    letter-spacing: 0.35em;
    text-transform: uppercase;
    color: var(--c-black);
    border-top: 1px solid var(--c-lime);
    padding-top: 0.75rem;
    margin-bottom: 2.5rem;
    display: inline-block;
  }

  .massive-title {
    position: relative;
    margin-bottom: 3rem;
    display: flex;
    flex-direction: column;
  }

  .ornament-bg {
    position: absolute;
    right: 0;
    top: 0;
    font-family: "Playfair Display", serif;
    font-size: clamp(6rem, 18vw, 14rem);
    font-weight: 900;
    font-style: italic;
    color: var(--c-warm);
    line-height: 1;
    user-select: none;
    pointer-events: none;
    z-index: 0;
  }

  .line {
    font-family: "Playfair Display", serif;
    line-height: 0.88;
    display: block;
    position: relative;
    z-index: 1;
  }

  .line-1 {
    font-size: clamp(2.8rem, 9vw, 7rem);
    font-weight: 400;
    font-style: italic;
    color: var(--c-hot);
  }

  .line-2 {
    font-size: clamp(3.5rem, 12vw, 9.5rem);
    font-weight: 900;
    letter-spacing: -0.02em;
    padding: 0.1em 0;
    color: var(--c-yellow);
  }

  .line-3 {
    font-size: clamp(1.4rem, 4vw, 3rem);
    font-weight: 400;
    font-style: italic;
    color: var(--c-teal);
    letter-spacing: 0.02em;
  }

  .divider {
    width: 80px;
    height: 1px;
    background: var(--c-lime);
    margin-bottom: 2rem;
  }

  .hero-text {
    font-family: "EB Garamond", serif;
    font-size: clamp(1.1rem, 2.2vw, 1.35rem);
    line-height: 1.75;
    color: var(--c-bg);
    max-width: 560px;
    margin-bottom: 2.5rem;
    font-style: italic;
  }

  .hero-text em {
    font-style: normal;
    font-weight: 500;
    background: linear-gradient(25deg, var(--c-lime) 40%, var(--c-lime) 60%);
    color: var(--c-white);
    padding: 0 3px;
  }

  .cta-button {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    font-family: "EB Garamond", serif;
    font-size: 0.85rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--c-bg);
    text-decoration: none;
    border: 1px solid currentColor;
    padding: 0.85rem 2rem;
    background-color: var(--c-hot);
    transition:
      background 0.2s,
      color 0.2s;
  }

  .cta-button:hover {
    background-color: var(--c-yellow);
    color: var(--c-black);
  }
</style>
